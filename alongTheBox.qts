var __interpretation_started_timestamp__;
var pi = 3.14159265;
var S;
var Sold;
var u;

var main = function()
{
	__interpretation_started_timestamp__ = Date.now();

	S = brick.sensor(A1).read();
	Sold = S;
	while (true) {
		u =  (S - brick.sensor(A1).read()); // + 5 * (Sold - brick.sensor(A1).read());
		Sold = brick.sensor(A1).read();
		brick.motor(M4).setPower(- u);
		brick.motor(M3).setPower(+ u);
		script.wait(30);
	}
}
